/**
 * @swagger
 * components:
 *   securitySchemes:
 *     bearerAuth:
 *       type: apiKey
 *       in: header
 *       schema: bearer
 *       bearerFormat: JWT
 *       name: Authorization
 *       description: >-
 *          Enter the token with the `Bearer: ` prefix, e.g. "Bearer abcde12345".
 */
